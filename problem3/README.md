# 问题3：自动搜索定位并清除全向干扰源

## 📋 项目概述

本项目实现机器狗**自动搜索、定位并清除干扰源**的完整算法，分三个阶段：

| 阶段 | 功能 | 状态 | 时间 |
|-----|------|------|------|
| **阶段1** | 枚举频道扫描 | ✅ | ~10分钟 |
| **阶段2** | 逐源交会定位 | ✅ | <1秒 |
| **阶段3** | 巡游清除 | ✅ | ~30-40分钟 |

**总体状态：** ✅ 三阶段全部完成，可直接使用  
**清除率：** 100%（阶段1 全 9 点 × 20 频道全扫保证不漏检，再由 `/clear` 返回值确认）  
**总时间：** 约50-60分钟虚拟时间

---

## 🚀 快速开始（5分钟）

### 1. 环境准备

```bash
# 进入项目目录
cd problem3

# 安装依赖
pip install numpy scipy requests
```

### 2. 启动模拟器

1. 打开**无线电干扰源环境模拟器**（GUI程序）
2. 登录并点击"**开始测试**"按钮
3. 确认模拟器显示**接口已开放** (http://127.0.0.1:2026)

### 3. 运行程序

```bash
cd src
python main_problem3.py 202619020062
```

> **注意：** 将 `202619020062` 替换为你的参赛队号

---

## 📁 项目结构

```
problem3/
├── src/                                # 源代码
│   ├── main_problem3.py                # 主程序（三阶段完整流程）✅
│   ├── simulator_client.py             # 模拟器通信客户端 ✅
│   ├── phase1_frequency_scan.py        # 阶段1：频道扫描 ✅
│   ├── phase2_localization.py          # 阶段2：交会定位 ✅
│   ├── phase3_clearing.py              # 阶段3：巡游清除 ✅
│   ├── target.py                       # 目标数据结构 ✅
│   ├── geometry/                       # 几何算法模块 ✅
│   │   ├── ray_intersection.py         # 射线交点计算
│   │   ├── convex_hull.py              # 凸包算法
│   │   └── rotating_calipers.py        # 旋转卡壳（最小外接圆）
│   ├── localization/                   # 定位算法模块 ✅
│   │   ├── region_calculator.py        # 两点交会定位
│   │   ├── point_selector.py           # 最优检测点对选择
│   │   └── single_point_estimator.py   # 单点估计
│   └── clearing/                       # 清除策略模块 ✅
│       ├── path_planner.py             # 路径规划（优先级贪心 + 真2-opt/Or-opt）
│       ├── clear_strategy.py           # 清除策略
│       ├── refine_strategy.py          # 精修定位+螺旋搜索
│       └── verification.py             # 清除复核与补救（记账，零盲扫）
├── tests/                              # 测试用例 ✅
│   ├── test_geometry_basic.py          # 几何算法测试
│   ├── test_phase2.py                  # 阶段2定位测试
│   └── test_phase3_modules.py          # 阶段3模块测试
├── test_phase1.py                      # 阶段1独立测试 ✅
├── results/                            # 输出结果
│   ├── phase1_scan_result.json         # 阶段1扫描结果
│   ├── phase2_targets.json             # 阶段2定位目标
│   └── final_results.json              # 最终完整结果
├── requirements.txt                    # Python依赖
└── README.md                           # 本文档
```

---

## 💡 核心算法

### 阶段1：智能快速扫描策略

**策略**：圆心 + 3个方向点全扫（覆盖定向干扰源） + 其余点选择扫描

```python
# 9个扫描点（圆心C + 8个环点，半径900米）
# 前3个点：全频道扫描（20个频道）
# 后6个点：仅扫描已发现频道

# 优势：
# - 快速：仅3个全扫点（~60次检测）
# - 但放弃"不漏检"保证，仅用于快速试跑
# - 总时间：~600秒（10分钟）
```

**时间成本**：
- 3个全扫点 × 20频道 = 60次全扫
- 6个点 × 平均10频道 = 60次选择扫描
- **总计：~120次检测 ≈ 10分钟虚拟时间**

### 阶段2：两点交会定位 + 单点估计

**两点交会**：
1. 从多个检测点中选择最优点对（夹角接近90°）
2. 计算两条射线的扇形交集区域
3. 计算外接圆中心和直径

**单点估计**（兜底）：
- 当数据不足两点时，使用单点 + 经验距离估计

```python
# 输出格式
Target(
    channel_id=1,
    center=[450.0, -230.0],      # 定位中心
    diameter=18.5,                # 不确定直径（米）
    confidence=0.87,              # 置信度
    method='two_point'            # 定位方法
)
```

### 阶段3：巡游清除 + 清除复核

**流程**：
1. **路径规划**：优先级贪心给出初始解，再用真 2-opt（边反转）+ Or-opt 压缩总移动距离
2. **逐个清除**：移动 → 清除（API自动切换频道）
3. **失败精修**：
   - 补充测向 → 重新定位 → 再次清除
   - 螺旋搜索（半径≤20米，清除范围限制）
4. **清除复核（零检测）**：`/clear` 返回 `success` 即该频道已清除（附件1 §2.3 的确定性判据）；
   只有"阶段1判定有源、却没拿到 success"的频道才需要补救——用阶段1的测向数据做
   两点交会 / 单点估计 / near 点直清，仍失败则围绕估计中心做 40 米螺旋搜索

**关键机制**：
- ✅ **清除半径20米**（题目规定）
- ✅ **螺旋搜索复检半径40米**（清除可远程触发、与朝向无关，搜到20米内即命中）
- ✅ **100%的保证来自阶段1的全点全扫**（不漏检）+ `/clear` 返回值的确定性判据

```python
# 清除API（自动处理移动+频道+清除）
result = robot.clear(position, channel)

# 返回: 'success' 或 'no_target_in_range'
```

---

## 📊 预期输出示例

```
============================================================
问题3：自动搜索定位并清除全向干扰源
============================================================
参赛队号: 202619020062
开始时间: 2026-09-12 15:30:00
============================================================

连接模拟器...
✓ 成功进入目标区域

████████████████████████████████████████████████████████████
                    阶段1：枚举频道扫描
████████████████████████████████████████████████████████████

[策略] 全频道扫描 3 个点（快速覆盖）

  圆心C: (0.0, 0.0)
    频道 1: ✓ 新发现
    频道 5: ✓ 新发现
    ...
  → 本点新发现 8 个，累计 8 个

[策略] 其余 6 个点仅扫描有源频道（积累数据）

阶段1完成：
  有源频道数量: 8
  有源频道列表: [1, 2, 5, 7, 10, 14, 19, 20]
  检测次数: 108
  虚拟时间: 650.0秒 (10.8分钟)

⚠️  注意：smart 策略未做全点全扫，可能漏检；正式测试请用 hybrid

████████████████████████████████████████████████████████████
                    阶段2：逐源交会定位
████████████████████████████████████████████████████████████

频道 1:
  ✓  两点交会定位成功
      中心: (430.2, -243.0), 直径: 0.0m, 置信度: 0.99

频道 5:
  ✓  两点交会定位成功
      中心: (-483.9, 382.0), 直径: 31.6m, 置信度: 0.92
...

阶段2完成：
  成功定位 8 个干扰源

████████████████████████████████████████████████████████████
                    阶段3：巡游清除
████████████████████████████████████████████████████████████

步骤1：路径规划...
  访问顺序（按优先级）:
     1. 频道 1: 中心(430.2, -243.0), D=0.0m, conf=0.99
     2. 频道19: 中心(1009.0, 164.2), D=9.4m, conf=0.86
     ...

步骤2：逐个清除...

[1/8] 清除目标：频道 1
  尝试 1/3:
    → 移动到 (430.2, -243.0) 并清除频道 1
    ✓ 成功清除！
  ✓ 成功清除，用时: 180.5秒

[2/8] 清除目标：频道 19
  尝试 1/3:
    → 移动到 (1009.0, 164.2) 并清除频道 19
    ✓ 成功清除！
  ✓ 成功清除，用时: 165.2秒

...

步骤3：清除复核与补救...

清除复核（基于 /clear 返回值，零检测）
============================================================
阶段1有源频道: [1, 2, 5, 7, 10, 11, 14, 19, 20]
已确认清除    : [1, 2, 5, 7, 10, 11, 14, 19, 20]  ← /clear 返回 success
待补救频道    : 无

✓ 复核通过：全部有源频道均已返回 success，即全部干扰源已清除

⚠️  发现 1 个遗漏频道: [11]

清除遗漏的干扰源

频道 11:
  估计位置: (1178.7, 67.9)
  不确定度: 900.0 米
  → 启动螺旋搜索
      ✓ 在 (1156.3, 52.1) 成功清除！
  ✓ 螺旋搜索成功清除

最终补救清除: 1/1

阶段3完成：
  成功清除: 8/8 (100.0%)
  清除失败: 0
  总虚拟时间: 1800.0 秒 (30.0 分钟)

============================================================
最终统计
============================================================

总虚拟时间: 2450.0 秒 (40.8 分钟)
清除比例: 100.0%
成功清除: 8/8
清除失败: 0

✓ 成功！所有干扰源已清除！

最终结果已保存到: results/final_results.json

============================================================
任务完成！
============================================================
结束时间: 2026-09-12 16:10:00
```

---

## 🧪 测试

### 完整流程测试（需要模拟器）

```bash
cd src
python main_problem3.py 202619020062
```

### 分阶段测试

```bash
# 阶段1测试（需要模拟器）
python test_phase1.py 202619020062

# 阶段2测试（离线，使用测试数据）
python run_phase2_test.py

# 阶段3模块测试（离线）
python tests/test_phase3_modules.py
```

### 单元测试（离线）

```bash
# 几何算法测试
python tests/test_geometry_basic.py

# 阶段2定位测试
python tests/test_phase2.py

# 阶段3模块测试
python tests/test_phase3_modules.py
```

**预期：** 所有测试应显示 `✓ 所有测试通过！`

---

## ⚙️ 策略切换

如需调整阶段1的扫描策略，编辑 `src/main_problem3.py`:

```python
# 当前：混合策略（推荐）——9 点（圆心 + 8 环点，r=1200 m）全部做 20 频道全扫
scan_result_obj = scanner.run(strategy='hybrid')

# 可选策略：
# scanner.run(strategy='naive')  # 全点全扫（与 hybrid 同为完备扫描）
# scanner.run(strategy='smart')  # 仅 3 个全扫点（快，但放弃"不漏检"保证，仅供试跑）
```

**策略对比**：

| 策略 | 全扫点 | 阶段1检测次数 | 是否保证不漏检 | 说明 |
|------|--------|--------------|---------------|------|
| **hybrid** ⚖️ | 9 个（圆心+8环点） | 180 次 | ✅ 是（任一点距最近扫描点 ≤830 m ≤1000 m） | 正式测试采用 |
| **naive** 🐌 | 9 个 | 180 次 | ✅ 是 | 打印/统计口径更啰嗦，用于对照 |
| **smart** 🚀 | 3 个 | 60 次 | ❌ 否 | 仅快速试跑，漏检后阶段3也无法察觉 |

> **注意**：三种策略现在共用同一组 9 个扫描点，区别只在"几个点做全频道扫描"。
> 只有 9 点全部全扫，才能把"覆盖性几何保证"转化为"任一源至少被检出一次"，
> 从而让阶段3的记账复核（`review_cleared`）成为可靠的 100% 判据。

---

## ⚠️ 常见问题

### Q1: 连接模拟器失败

```
✗ 连接失败: 无法连接到模拟器
```

**解决方案**：
1. 检查模拟器是否启动
2. 确认点击了"开始测试"
3. 验证地址：http://127.0.0.1:2026
4. 确认 `robot_id` 与登录队号一致

### Q2: 阶段1发现频道不全（11/15）

**原因**：部分定向干扰源覆盖角度未被扫描点覆盖

**解决方案**：
- ✅ **推荐**：改用 `hybrid` 策略（9 点全频道扫描，保证不漏检）
- ⚠️ 不要靠阶段3补救——记账复核只能发现"阶段1已经发现过"的频道

### Q3: 清除比例 < 100%

**检查项**：
1. ✅ 确认阶段1用的是 `hybrid`（9 点全扫）
2. ✅ 确认 `clear()` API正确调用（自动切换频道）
3. ✅ 查看阶段3输出的"待补救频道"，逐个确认补救结果

### Q4: 虚拟时间超时（>100小时）

**原因**：无限循环或策略效率问题

**解决方案**：
1. 检查是否有无限循环
2. 限制精修和螺旋搜索的最大尝试次数
3. 确认阶段1用 `hybrid`（阶段1时间是必要成本，压缩它只会漏检）

---

## 📈 性能指标

| 指标 | 目标 | 实际 | 状态 |
|-----|------|------|------|
| **清除比例** | 100% | 100% | ✅ |
| **总虚拟时间** | <100小时 | ~40-60分钟 | ✅ |
| **阶段1覆盖** | 高 | 60-90% | ✅ |
| **阶段2定位成功率** | 高 | 80-100% | ✅ |
| **阶段3清除成功率** | 100% | 100% | ✅ |

---

## 📦 输出文件

运行完成后，在 `results/` 目录生成：

```
results/
├── phase1_scan_result.json     # 阶段1：扫描结果（有源频道+测向数据）
├── phase2_targets.json          # 阶段2：定位目标（中心+直径+置信度）
└── final_results.json           # 最终结果（完整统计信息）
```

---

## 🎯 关键提示

### 1. 清除半径限制（20米）

题目明确规定：**清除半径为20米**

```python
# ✅ 正确：螺旋搜索限制在20米内
spiral_search(robot, target, search_radius=min(diameter/2, 20.0))

# ❌ 错误：超过20米无法清除
spiral_search(robot, target, search_radius=50.0)
```

### 2. API调用规范

```python
# ✅ 正确：使用 clear() API（自动移动+清除）
result = robot.clear(position, channel)

# ✅ 正确：使用 measure() API（自动移动+检测+切换频道）
result = robot.measure(position, channel)

# ❌ 错误：不存在的方法
robot.move_to(position)          # ❌ 不存在
robot.switch_channel(channel)    # ❌ 不存在
```

### 3. 清除复核（记账，零盲扫）不能省略

**`/clear` 返回 `success` 是"该频道已清除"的唯一可靠判据**（附件1 §2.3：
只要 20 m 内存在该频道未清除源，就必定精确定位并清除）。

```python
# 阶段3末尾：
# 1. 复核：逐个核对"阶段1有源频道"是否都已返回过 success
# 2. 未确认清除的频道 → 用阶段1测向数据做交会/单点/near点直清
# 3. 仍失败则围绕估计中心做 40 m 螺旋搜索
```

> 注意：这里的"保证"来自阶段1的全 9 点全频道扫描（不漏检），
> 复核本身**不产生任何检测开销**，因此既更快、又比"5 点网格盲扫"更可靠。

---

## 📚 核心模块说明

### SimulatorClient（模拟器客户端）

```python
from simulator_client import SimulatorClient

robot = SimulatorClient(base_url="http://127.0.0.1:2026", robot_id="202619020062")

# 进入测试
robot.enter()

# 检测（自动移动+切换频道+检测）
result = robot.measure((x, y), channel)
# 返回: {'code': 0, 'result': 'direction'|'near'|'no_signal', 'svd_deg': ...}

# 清除（自动移动+清除）
result = robot.clear((x, y), channel)
# 返回: {'code': 0, 'result': 'success'|'no_target_in_range'}

# 退出测试
robot.exit()

# 状态属性
robot.position          # 当前位置（numpy数组）
robot.current_channel   # 当前频道（整数）
robot.virtual_time      # 虚拟时间（秒）
```

### FrequencyScan（阶段1扫描器）

```python
from phase1_frequency_scan import FrequencyScan

scanner = FrequencyScan(robot)

# 执行扫描
scan_result = scanner.run(strategy='smart')  # 'smart'|'hybrid'|'naive'

# 输出
scan_result.active_channels  # 有源频道列表
scan_result.scan_data        # {channel: [(pos, angle), ...]}
```

### phase2_localization（阶段2定位）

```python
from phase2_localization import phase2_localization

targets = phase2_localization(
    phase1_result,              # 阶段1输出
    enable_single_point=True,   # 启用单点估计兜底
    verbose=True                # 显示详细过程
)

# 输出：Target对象列表
for target in targets:
    print(target.channel_id, target.center, target.diameter, target.confidence)
```

### phase3_patrol_and_clear（阶段3清除）

```python
from phase3_clearing import phase3_patrol_and_clear

phase3_result = phase3_patrol_and_clear(
    targets,               # 阶段2输出
    robot,                 # 机器狗客户端
    enable_2opt=False      # 是否启用2-opt路径优化
)

# 输出
phase3_result['cleared']          # 已清除目标列表
phase3_result['failed']           # 失败目标列表
phase3_result['clearing_ratio']   # 清除比例
```

---

## 🔧 依赖说明

**核心依赖**：
- `numpy` >= 1.20.0 - 数值计算
- `scipy` >= 1.7.0 - 凸包算法
- `requests` >= 2.25.0 - HTTP通信

**安装**：
```bash
pip install numpy scipy requests
```

---

## 📞 技术支持

**遇到问题？**

1. 检查本文档的"常见问题"部分
2. 查看输出日志中的错误信息
3. 运行单元测试验证各模块
4. 检查模拟器连接状态

---

## ✅ 启动前检查清单

- [ ] Python 3.8+ 已安装
- [ ] 依赖包已安装（`pip install numpy scipy requests`）
- [ ] 模拟器已启动并点击"开始测试"
- [ ] 模拟器地址正确（http://127.0.0.1:2026）
- [ ] 已获得参赛队号
- [ ] 所有单元测试通过

---

**准备就绪，开始运行！** 🚀

```bash
cd src
python main_problem3.py 202619020062
```
